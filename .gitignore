# Python virtualenvs / bytecode
.venv/
venv/
__pycache__/
*.pyc
*.pyo

# OS cruft
.DS_Store

# local secrets safety net
*.pem
*.key
*kubeconfig*
.env

# per-run scenario pcaps (phase6) -- every automated run produces one; only
# deliberately curated reference captures get promoted into traces/ and committed
traces/scenario-runs/

# ad hoc manual captures (CLI 'c' key) -- same reasoning as scenario-runs/ above
traces/manual/
